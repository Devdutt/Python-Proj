# Lists
# Dictionaries
# Tuples (are like lists which as non-changable)
# tuples have () brackets
# Tuples like Strings are immutable

# t = tuple()

(a, b) = ("jack", 4)
print(b)

d = {"h": 10, "b": 19, "c": 20}
# t = list()
t = sorted(d.items())

for i, j in t:
    print(i, j)

